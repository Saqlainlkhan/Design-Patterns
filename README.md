# Design-Patterns
This project implements a modular, extensible text-based document editor application demonstrating the practical use of 11 design patterns. The application handles documents composed of various elements (text, images, etc.) through a console-based interface with full undo/redo capability. 
